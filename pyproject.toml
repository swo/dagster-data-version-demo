[project]
name = "dagster-data-version-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dagster>=1.13.18",
    "dagster-dg-cli>=1.13.18",
    "dagster-webserver>=1.13.18",
]

[tool.dg]
directory_type = "project"
# project.root_module = "main"
